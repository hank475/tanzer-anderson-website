name: Build email background assets

on:
  push:
    branches: [main]
    paths:
      - 'email-standard/assets/tanzer-background-01.jpg.b64part*'
      - '.github/workflows/build-email-background-assets.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  decode:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Assemble and decode premium Gmail background
        shell: bash
        run: |
          set -euo pipefail
          cat email-standard/assets/tanzer-background-01.jpg.b64part* \
            | base64 --decode \
            > email-standard/assets/tanzer-background-01.jpg
          file email-standard/assets/tanzer-background-01.jpg

      - name: Commit generated binary asset
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add email-standard/assets/tanzer-background-01.jpg
          if git diff --cached --quiet; then
            echo "Generated asset is already current."
            exit 0
          fi
          git commit -m "Build premium Gmail background asset"
          git push
